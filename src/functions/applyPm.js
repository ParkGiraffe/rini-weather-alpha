

export default function applyPm(pmObj) {
  console.log(pmObj);
  // const forecastData = [];
  // for (let i = 0; i < 60; i = i + 6) {
  //   forecastData.push(weatherObj.response.body.items.item[i]);
  // }

  // const T1H = forecastData[4].fcstValue; // 현재 기온
  // const RN1 =
  //   forecastData[2].fcstValue === '강수없음' ? 0 : forecastData[2].fcstValue; // 1시간 강수량

  // return {T1H: T1H, RN1: RN1};
}
